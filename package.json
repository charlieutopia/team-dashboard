{
  "name": "team-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=20", "pnpm": ">=9" },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "scanner:daily": "node packages/scanner/dist/run-daily.js",
    "scanner:weekly": "node packages/scanner/dist/run-weekly.js",
    "scanner:probe-tone": "node packages/scanner/dist/probe-tone.js",
    "scanner:probe-weekly": "node packages/scanner/dist/probe-weekly.js"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
